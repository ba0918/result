# Option型の実装 + Result型へのtranspose()の実装 - 詳細計画書

**作成日時**: 2025-07-13 01:00:58  
**プロジェクト**: mizumi/result  
**実装対象**: Option型の完全実装とResult型へのtranspose()メソッド追加

## 1. 分析結果概要

### 1.1 現在のコードベース状況
- **Result型**: 充実した実装が完了済み（Ok/Err、各種メソッド）
- **アーキテクチャ**: PSR-4準拠、テスト充実、PHPDoc完備
- **技術スタック**: PHP 8.4+、PHPUnit 12.2、PHPStan 2.1
- **コーディング規約**: immutable、final、readonly、日本語コメント

### 1.2 実装する機能
1. **Option型インターフェース**: `Option<T>`
2. **Some実装クラス**: 値を持つ場合
3. **None実装クラス**: 値を持たない場合  
4. **transpose()メソッド**: Result/Option相互変換

## 2. Option型仕様設計

### 2.1 インターフェース定義
```php
interface Option
{
    // 基本検査
    public function isSome(): bool;
    public function isNone(): bool;
    
    // 値変換
    public function map(callable $fn): Option;
    public function mapOr(callable $fn, mixed $default): mixed;
    public function mapOrElse(callable $fn, callable $defaultFn): mixed;
    
    // モナド操作
    public function andThen(callable $fn): Option;
    public function filter(callable $predicate): Option;
    
    // 値取得
    public function unwrap(): mixed;
    public function unwrapOr(mixed $default): mixed;
    public function unwrapOrElse(callable $fn): mixed;
    public function expect(string $message): mixed;
    
    // 副作用
    public function inspect(callable $fn): Option;
    
    // 結合操作
    public function or(Option $opt): Option;
    public function orElse(callable $fn): Option;
    public function and(Option $opt): Option;
    
    // 値確認
    public function contains(mixed $value): bool;
    
    // 相互変換
    public function transpose(): Result;
    public function okOr(mixed $err): Result;
    public function okOrElse(callable $fn): Result;
}
```

### 2.2 Some実装クラス
```php
final class Some implements Option
{
    public function __construct(private readonly mixed $value) {}
    public static function of(mixed $value): self {}
    // 全メソッド実装...
}
```

### 2.3 None実装クラス
```php
final class None implements Option
{
    public static function instance(): self {}
    // 全メソッド実装...
}
```

## 3. transpose()メソッド仕様

### 3.1 Result型への追加
```php
interface Result
{
    // 既存メソッド...
    
    /**
     * Result<Option<T>, E> → Option<Result<T, E>> への変換
     *
     * @return Option<Result<T, E>>
     */
    public function transpose(): Option;
}
```

### 3.2 変換ルール

#### Option<Result<T, E>> → Result<Option<T>, E>
- `None` → `Ok(None)`
- `Some(Ok(value))` → `Ok(Some(value))`
- `Some(Err(error))` → `Err(error)`

#### Result<Option<T>, E> → Option<Result<T, E>>
- `Ok(None)` → `None`
- `Ok(Some(value))` → `Some(Ok(value))`
- `Err(error)` → `Some(Err(error))`

## 4. 実装方針とアーキテクチャ

### 4.1 設計原則
- **Immutability**: 全プロパティをreadonly
- **Type Safety**: PHPDocによるGenerics表現
- **継承禁止**: 全クラスをfinal
- **一貫性**: Result型と同様のパターン

### 4.2 ディレクトリ構造
```
src/
├── Result.php         (既存)
├── Ok.php            (既存)  
├── Err.php           (既存)
├── Option.php        (新規)
├── Some.php          (新規)
├── None.php          (新規)
└── Exception/
    └── UnwrapException.php (既存)
```

### 4.3 名前空間とパッケージ
- 名前空間: `Mizumi\Result`
- autoload: 既存のPSR-4設定を流用
- 例外クラス: `UnwrapException`を共用

## 5. 詳細実装タスク

### 5.1 フェーズ1: Option型基本実装
- [x] `Option.php`インターフェース作成
- [x] `Some.php`実装クラス作成
- [x] `None.php`実装クラス作成
- [x] 基本メソッド実装（isSome, isNone, map, unwrap系）

### 5.2 フェーズ2: 高度なメソッド実装
- [x] モナド操作（andThen, filter）
- [x] 結合操作（or, orElse, and）
- [x] 値確認（contains）
- [x] 副作用（inspect）

### 5.3 フェーズ3: Result連携機能
- [x] `Option::transpose()`実装
- [x] `Result::transpose()`実装
- [x] `Option::okOr()`/`okOrElse()`実装

### 5.4 フェーズ4: テスト実装
- [x] 基本機能テスト（33ケース実装）
- [x] transpose()テスト（15ケース実装）
- [x] エッジケーステスト（包括的実装）
- [x] パフォーマンステスト（含む）

### 5.5 フェーズ5: ドキュメント更新
- [x] 実装記録作成（`docs/implement/implement_20250713_010058.md`）
- [x] プロジェクトメモリ更新（`CLAUDE.md`）
- [x] 使用例追加

## 6. ファイル変更計画

### 6.1 新規作成ファイル
1. **src/Option.php**: インターフェース定義
2. **src/Some.php**: Some実装クラス
3. **src/None.php**: None実装クラス
4. **tests/OptionBasicTest.php**: 基本機能テスト
5. **tests/OptionAdvancedTest.php**: 高度機能テスト
6. **tests/TransposeTest.php**: transpose()専用テスト

### 6.2 修正ファイル
1. **src/Result.php**: transpose()メソッド追加
2. **src/Ok.php**: transpose()実装
3. **src/Err.php**: transpose()実装
4. **docs/spec/specification.md**: Option型仕様追加

### 6.3 参考ファイル（変更なし）
- composer.json（autoload設定は流用）
- phpstan.neon（静的解析設定）
- phpunit.xml（テスト設定）

## 7. テスト戦略

### 7.1 単体テスト（PHPUnit）
- **OptionBasicTest**: 基本メソッド（30テストケース）
- **OptionAdvancedTest**: 高度メソッド（25テストケース）
- **TransposeTest**: 相互変換（20テストケース）
- **EdgeCaseTest**: エッジケース（15テストケース）

### 7.2 静的解析（PHPStan）
- レベル最大での型チェック
- Generics表現の適切性確認
- 戻り値型の一貫性確認

### 7.3 統合テスト
- Result/Option間の連携動作確認
- 実用的なユースケーステスト
- パフォーマンス測定

### 7.4 テストカバレッジ目標
- **基本メソッド**: 100%カバレッジ
- **エッジケース**: 95%以上
- **エラーパス**: 100%カバレッジ

## 8. リスク分析と対策

### 8.1 技術的リスク

#### リスク1: PHPでのGenerics表現の限界
- **影響**: 型安全性の制約
- **対策**: PHPDocによる詳細な型注釈
- **軽減**: PHPStanによる静的解析強化

#### リスク2: None実装のシングルトンパターン
- **影響**: メモリ効率とthread-safety
- **対策**: static instanceメソッドによる制御
- **軽減**: immutabilityによる安全性確保

#### リスク3: transpose()の複雑性
- **影響**: バグの混入リスク
- **対策**: 段階的実装とテスト充実
- **軽減**: Rust仕様の忠実な再現

### 8.2 設計リスク

#### リスク1: Result型との一貫性
- **影響**: API使用時の混乱
- **対策**: 命名規則とパターンの統一
- **軽減**: 詳細な仕様書とサンプル

#### リスク2: 既存コードとの互換性
- **影響**: 破壊的変更の可能性
- **対策**: 既存ファイルの慎重な修正
- **軽減**: 十分なテストによる検証

### 8.3 パフォーマンスリスク

#### リスク1: オブジェクト生成オーバーヘッド
- **影響**: 頻繁な操作での性能劣化
- **対策**: 軽量な実装と最適化
- **軽減**: ベンチマークによる測定

## 9. 実装順序と優先度

### 高優先度（必須機能）
1. Option基本インターフェース
2. Some/None基本実装
3. 基本メソッド（isSome, isNone, map, unwrap）
4. transpose()基本実装

### 中優先度（重要機能）
1. 高度なメソッド（andThen, filter, or, orElse）
2. 詳細テスト実装
3. 静的解析対応

### 低優先度（補完機能）
1. ドキュメント詳細化
2. パフォーマンス最適化
3. 追加サンプル作成

## 10. 成功指標

### 10.1 機能面
- [ ] Option型の全メソッドが動作
- [ ] transpose()による相互変換が成功
- [ ] Result型との連携が適切

### 10.2 品質面
- [ ] テストカバレッジ95%以上
- [ ] PHPStan最大レベルでエラーなし
- [ ] 既存テストが全て通過

### 10.3 ドキュメント面
- [ ] 仕様書の更新完了
- [ ] 使用例の充実
- [ ] API一貫性の確保

## 11. 参考資料

### 11.1 Rust標準ライブラリ
- Option型仕様: https://doc.rust-lang.org/std/option/enum.Option.html
- Result型仕様: https://doc.rust-lang.org/std/result/enum.Result.html
- transpose()仕様: 両型での相互変換メソッド

### 11.2 実装パターン
- PHP Generics表現: PHPDocによる型注釈
- エラーハンドリング: UnwrapException統一
- テストパターン: 既存テストとの一貫性

## 12. まとめ

この実装により、PHPでRust風のOption/Result型による堅牢なエラーハンドリングが可能になります。transpose()メソッドにより、ネストした型の相互変換も簡潔に記述できるようになり、関数型プログラミングのパターンがPHPでより活用しやすくなります。

実装は段階的に進め、各フェーズでの十分なテストにより品質を確保します。既存のResult型との一貫性を保ちつつ、Rustの仕様に忠実な実装を目指します。